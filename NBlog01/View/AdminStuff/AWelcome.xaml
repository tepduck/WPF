<UserControl x:Class="NBlog01.View.AdminStuff.AWelcome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NBlog01.View.AdminStuff"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border Style="{StaticResource BorderStyle}">
            <StackPanel MinHeight="120"
                        MinWidth="200">
                <Label Content="{DynamicResource Welcome}"
                        TextBlock.Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                        Margin="0 15 0 20"                           
                        FontSize="20" 
                        FontWeight="Bold"/>
                <Button Margin="10 0 10 0"
                        Command="{Binding SetAdminwindow}"
                        Content="Окно управления"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
